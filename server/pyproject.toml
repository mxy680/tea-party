[tool.poetry]
name = "server"
version = "0.1.0"
description = ""
authors = ["Mark <mark@mxy680.net:>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.7.1,<4.0"
openai = "^1.37.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
